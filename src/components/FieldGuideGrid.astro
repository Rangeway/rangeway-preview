---
import { EXPERIENCE_PRINCIPLES } from "../data/site-content";
import ResponsiveImage from "./ResponsiveImage.astro";
import type { FieldGuideImages } from "./homepage-images";

interface Props {
  images: FieldGuideImages;
}

const { images } = Astro.props;
---

<section class="field-guide section" aria-labelledby="field-guide-title">
  <div class="container">
    <div class="kicker reveal">
      <span class="kicker__label">The Field Guide</span>
    </div>

    <div class="section-head reveal">
      <h2 id="field-guide-title">Hospitality, built into every stop.</h2>
      <p class="section-head__lede">
        Four practical principles shape every location, from the light inside to the moment a
        driver pulls away. Together, they make hospitality part of the operating standard.
      </p>
    </div>

    <ol class="field-guide__grid">
      {EXPERIENCE_PRINCIPLES.map((principle, index) => {
        const image = images[index];
        return (
          <li>
            <figure>
              <div class="field-guide__frame">
                <ResponsiveImage
                  src={image.src}
                  alt={image.alt}
                  loading="lazy"
                  sizes="(max-width: 700px) 100vw, 50vw"
                  style={image.focalPosition ? `object-position: ${image.focalPosition}` : undefined}
                />
              </div>
              <figcaption>{image.caption}</figcaption>
            </figure>
            <h3>{principle.title}</h3>
            <p>{principle.body}</p>
          </li>
        );
      })}
    </ol>
  </div>
</section>

<style>
  .field-guide {
    background: var(--surface-alt);
  }

  .field-guide figcaption {
    font-family: var(--font-heading);
    font-size: var(--fs-caption);
    font-weight: 700;
    letter-spacing: var(--ls-allcaps);
    text-transform: uppercase;
  }

  .field-guide__grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-8) clamp(24px, 4vw, 64px);
    margin: 0;
    padding: 0;
    list-style: none;
    counter-reset: field-guide;
  }

  .field-guide__grid > li {
    counter-increment: field-guide;
  }

  .field-guide figure {
    margin: 0 0 var(--space-5);
  }

  .field-guide__frame {
    overflow: hidden;
    aspect-ratio: 3 / 2;
    background: var(--surface-page);
  }

  .field-guide__frame :global(picture) {
    display: block;
    width: 100%;
    height: 100%;
  }

  .field-guide__frame :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .field-guide figcaption {
    padding-top: var(--space-3);
    color: var(--color-text-muted);
  }

  .field-guide h3::before {
    content: counter(field-guide, decimal-leading-zero) " / ";
    color: var(--color-oxide);
  }

  .field-guide p {
    max-width: 44ch;
    margin: var(--space-3) 0 0;
    color: var(--color-text-muted);
  }

  @media (max-width: 700px) {
    .field-guide__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
